---
import { Image } from 'astro:assets';
import entrada from '../assets/img/casino-entrada.png';

const salonTiles = [
  { src: '/images/20260609_123711.jpg', caption: 'El salón', pos: 'center', size: 'sm' },
  { src: '/images/20260609_123728.jpg', caption: 'Mesas preparadas para ti', pos: 'center', size: 'sm' },
  { src: '/images/20260609_123932.jpg', caption: 'La barra, siempre lista', pos: 'center top', size: 'sm' },
  { src: '/images/20260609_123936.jpg', caption: 'Nuestro equipo al pie del cañón', pos: 'center top', size: 'wide' },
];
---

<section id="galeria" class="gallery-section">
  <div class="gallery-inner">
    <header class="gallery-head reveal">
      <p class="eyebrow">• Galería •</p>
      <h2 class="section-title">Un paseo por El Casino</h2>
      <p class="section-lead">
        Buena mesa y el calor de siempre en el corazón de El Bonillo.
      </p>
    </header>

    <div class="bento reveal">
      <figure class="tile tile--lg">
        <Image
          src={entrada}
          alt="Nuestra fachada en la Plaza Mayor"
          class="tile-img"
          style="object-position:center"
          loading="lazy"
          widths={[400, 800, 1200]}
          sizes="(max-width: 768px) 100vw, 50vw"
        />
        <figcaption class="tile-caption">Nuestra fachada en la Plaza Mayor</figcaption>
      </figure>
      {salonTiles.map((tile) => (
        <figure class:list={['tile', `tile--${tile.size}`]}>
          <img
            src={tile.src}
            alt={tile.caption}
            class="tile-img"
            style={`object-position:${tile.pos}`}
            loading="lazy"
          />
          <figcaption class="tile-caption">{tile.caption}</figcaption>
        </figure>
      ))}
    </div>
  </div>
</section>

<style>
  .gallery-section {
    background: linear-gradient(180deg, var(--color-bg) 0%, var(--color-secondary) 35%, var(--color-secondary) 100%);
    padding: 5rem 1.5rem;
  }

  .gallery-inner {
    max-width: 72rem;
    margin: 0 auto;
  }

  .gallery-head {
    text-align: center;
    margin-bottom: 3.5rem;
  }

  .bento {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;
  }

  .tile {
    position: relative;
    margin: 0;
    overflow: hidden;
    border-radius: 1.25rem;
    border: 1px solid rgba(255, 255, 255, 0.07);
    min-height: 14rem;
    cursor: pointer;
  }

  .tile-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.6s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .tile::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(10, 10, 10, 0.85) 0%, transparent 55%);
    opacity: 0.85;
    transition: opacity 0.4s ease;
  }

  .tile-caption {
    position: absolute;
    left: 1.1rem;
    bottom: 1rem;
    right: 1.1rem;
    z-index: 1;
    color: var(--color-text);
    font-family: var(--font-serif);
    font-size: 1.05rem;
    transform: translateY(0.4rem);
    opacity: 0;
    transition:
      transform 0.4s ease,
      opacity 0.4s ease;
  }

  .tile:hover .tile-img,
  .tile:focus-within .tile-img {
    transform: scale(1.06);
  }

  .tile:hover::after,
  .tile:focus-within::after {
    opacity: 1;
  }

  .tile:hover .tile-caption,
  .tile:focus-within .tile-caption {
    transform: translateY(0);
    opacity: 1;
  }

  /* En dispositivos táctiles (sin hover) mostramos la leyenda siempre */
  @media (hover: none) {
    .tile-caption {
      opacity: 1;
      transform: none;
    }
    .tile::after {
      opacity: 1;
    }
  }

  @media (min-width: 768px) {
    .gallery-section {
      padding: 7rem 3rem;
    }
    .bento {
      grid-template-columns: repeat(4, 1fr);
      grid-auto-rows: 13rem;
    }
    .tile {
      min-height: 0;
    }
    .tile--lg {
      grid-column: span 2;
      grid-row: span 2;
    }
    .tile--sm {
      grid-column: span 2;
    }
    .tile--wide {
      grid-column: span 2;
      grid-row: span 1;
    }
  }
</style>
